# Secrets and machine-generated state — never commit.
.env.prod
*.env.prod

# Terraform (added in Phase 3)
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Ansible (added in Phase 4)
*.retry
